<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /dsatable2/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /dsatable2/index.html [L]
</IfModule>

